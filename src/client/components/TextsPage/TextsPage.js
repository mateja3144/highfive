import React from 'react';
import style from './style.css';

export const TextsPage = () => (
    <section className={style.section}>
        <h2 className={style.title}>Teksty</h2>
    </section>
);

TextsPage.propTypes = {
};

export default TextsPage;
